<div class="subjects">
  <% @subjects.each do |subject| %>
    <ul class="list-group">
      <a><%= link_to subject.name, subject_path(subject.id),:remote => true, :class => "subjects" %></a>
    </ul>
  <% end %>
</div>
